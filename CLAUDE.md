# 项目上下文

## 项目概述
个人博客写作为核心、随笔动态表达与成长图谱可视化为主线、低干扰交流为辅助的现代化博客平台。

## 技术栈
- 前端：Vue3 + Vite + TypeScript + Pinia + Vue Router + Axios + ECharts
- 后端：Spring Boot 3 + Spring Security + MyBatis-Plus
- 数据库：MySQL（开发联调支持 H2 内存库）
- 构建：npm / Maven

## 重要决策
- 采用前后端分离架构。
- 登录态优先使用 Session/Cookie。
- 一期优先交付“博客写作 + 随笔动态 + 成长可视化”闭环，深度社交功能后置。
- 前端视觉遵循 AGENTS.md 约束（禁紫色系、避免对称三卡、口语化短文案）。
- 首页采用公开访问模式，受保护功能在未登录状态下统一提示“请登录后使用”并引导跳转登录。
- 首页首屏文案区采用居中布局，移除首屏快捷按钮，保留下方模块入口。
- 成长图谱二阶段采用“主时间线 + 技能雷达 + 周内活跃分布 + 状态结构 + 闭环分析（产出/反馈/迭代）”统一口径。
- 二期社交模块采用“先随笔互动、再关注私信”的分段推进策略。

## 已知问题
- 当前命令行 `curl -d` 发送中文 JSON 可能出现编码异常（Invalid UTF-8 start byte），英文 payload 正常。
- 成长图谱页面打包后 chunk 偏大（ECharts 导致），后续可按图表模块做进一步拆包优化。
- Maven 全局 settings.xml 存在非标准 `profile` 标签告警，不影响构建与运行。
- 首页视觉已按 `web_style.jpg` 完成重构并通过构建，需在 375 / 768 / 1440 三档继续做人工验收。
- 成长页“个人成就”中的博客点赞暂未接入后端数据源，当前前端固定展示为 0。

## 下一步计划
1. 二期社交模块：推进关注、私信。
2. 低干扰机制：默认弱提醒、安静时段、通知汇总。
3. 后台管理基础：用户、内容、权限管理。
4. 安全与性能加固：输入校验、权限边界、索引与慢查询优化。
5. 部署完善：补齐 MySQL 正式环境配置与容器化方案。

## 二期当前进度（2026-05）
- 已完成：随笔互动（点赞 / 评论）后端接口与前端联调、随笔可见范围第一版（公开 / 仅自己 / 圈层）。
- 待开发：关注 / 私信 / 低干扰提醒机制。

