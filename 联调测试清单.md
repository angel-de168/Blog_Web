# 前后端联调测试清单

> 目标：明确当前已实现能力的联调状态，快速定位阻塞项，并为下一步开发计划提供依据。

## 0. 联调前准备（P0）

- [x] 后端启动成功（`8080`），健康检查可用（`/api/health`）

  直接访问：[localhost:8080/api/health](http://localhost:8080/api/health) 浏览器显示：{"success":true,"message":"ok","data":{"status":"UP","time":"2026-05-04T10:34:19.709326600"}}

- [x] 前端启动成功（Vite），可访问首页

  成功访问首页

- [x] MySQL 已建库并已执行 `V1__init_schema.sql`、`V2__add_moment_table.sql`

  已执行，数据库成功创建表

- [x] 浏览器 DevTools 已打开（Network + Console）

---

## 1. 鉴权链路（P0）

### 1.1 注册
- [x] 进入 `/login`，切换到“注册”

- [x] 输入用户名/邮箱/密码并提交

- [x] 请求：`POST /api/auth/register`

- [x] 期望：返回成功；可进入登录态或继续登录

  实际状态：已改为“注册成功后停留在登录页并提示‘注册成功，请登录’”，不再直接跳转博客页面。

### 1.2 登录
- [x] 在 `/login` 输入账号密码
- [x] 请求：`POST /api/auth/login`
- [x] 期望：登录成功，跳转到 `redirect`（默认 `/notes`）

### 1.3 当前用户恢复
- [x] 登录后刷新受保护页（如 `/notes`）

- [x] 请求：`GET /api/auth/me`

  已调整为受保护路由每次进入都做 `GET /api/auth/me` 校验，便于 Network 直接观测会话恢复。

- [x] 期望：用户态恢复，页面不回登录页

### 1.4 登出
- [x] 点击导航“退出”

- [x] 请求：`POST /api/auth/logout`

- [x] 期望：跳回首页；再次访问 `/notes` 会被引导登录

  说明：首页未登录点击受保护导航会弹出“请登录后使用”的居中遮罩提示，可直接点“去登录”。

---

## 2. 博客 Notes（P0）

### 2.1 列表与筛选
- [x] 打开 `/notes`
- [x] 请求：`GET /api/notes?page=1&size=10&keyword&status`
- [x] 期望：列表正常显示；关键词/状态筛选生效

### 2.2 新建
- [x] 点击“写新博客”进入 `/notes/new`
- [x] 保存后请求：`POST /api/notes`
- [x] 期望：返回列表，出现新记录

### 2.3 编辑
- [x] 点击某条“编辑”进入 `/notes/:id/edit`
- [x] 请求：`GET /api/notes/{id}` + `PUT /api/notes/{id}`
- [x] 期望：修改后列表内容更新

### 2.4 删除
- [x] 点击“删除”
- [x] 请求：`DELETE /api/notes/{id}`
- [x] 期望：删除成功，列表刷新，总数变化

---

## 3. 随笔 Moments（P0）

### 3.1 列表与筛选
- [x] 打开 `/moments`
- [x] 请求：`GET /api/moments?page=1&size=10&keyword`
- [x] 期望：列表正常显示；关键词筛选生效

### 3.2 新建 / 编辑 / 删除
- [x] 新建：`POST /api/moments`
- [x] 编辑：`GET /api/moments/{id}` + `PUT /api/moments/{id}`
- [x] 删除：`DELETE /api/moments/{id}`
- [x] 期望：三条链路都可闭环，列表状态一致

---

## 4. 成长图谱 Growth（P0）

- [x] 打开 `/growth`，默认请求 `GET /api/growth/overview?days=120`
- [x] 切换 30/90/120 天分别触发请求
- [x] 期望：时间线与雷达图均更新；空数据时页面不报错

---

## 5. 首页与权限门槛（P0）

- [x] 未登录访问首页，点击“博客/随笔/成长图谱”

- [x] 期望：提示“请登录后使用”并可跳登录

  已改为居中遮罩提示，背景内容弱化，可点“去登录”或“知道了”。

- [x] 登录后重复点击

- [x] 期望：可正常进入对应页面

---

## 6. 当前能力盘点（联调结论区）

### 已实现
- [x] 鉴权基础（注册/登录/登出/当前用户）
- [x] Notes：CRUD + 筛选
- [x] Moments：CRUD + 筛选
- [x] Growth：overview 图表（30/90/120）
- [x] 首页登录门槛与路由守卫

### 部分实现
- [x] 注册流程在登录页内切换模式（无独立注册页）
- [x] 列表接口支持分页参数，前端已补齐上一页/下一页与页码显示（Notes/Moments）

### 未实现
- [ ] 点赞
- [ ] 评论
- [ ] 关注
- [ ] 私信
- [ ] 低干扰提醒（分级/安静时段/通知汇总）
- [ ] 后台管理（用户/内容/权限）

---

## 7. 问题记录模板

```md
### 联调问题 #01
- 时间：
- 页面：
- 操作步骤：
- 实际结果：
- 期望结果：
- 接口与参数：
- 响应体：
- 控制台报错：
- 结论：前端 / 后端 / 数据 / 配置
```
