# DEVELOPMENT.md

## 1. 文档目的

本文件用于沉淀当前项目的开发过程、阶段性结果与后续开发安排，便于按步骤继续推进。

---

## 2. 项目目标（当前版本）

围绕个人博客写作，先完成“记录 → 表达 → 复盘 → 可视化”闭环：
1. 记录：笔记增删改查与标签管理
2. 表达：随笔动态发布与管理
3. 复盘：按状态和关键词筛选内容
4. 可视化：成长时间线 + 技能雷达 + 闭环分析

---

## 3. 已完成开发步骤（按实施顺序）

### Step 1：工程初始化与技术栈落地
- 前端：Vue3 + Vite + TypeScript + Pinia + Vue Router + Axios + ECharts
- 后端：Spring Boot 3 + Spring Security + MyBatis-Plus
- 数据库：MySQL（开发联调支持 H2）
- 完成前后端分离工程骨架搭建

### Step 2：前端基础能力搭建
- 完成路由与页面框架：`/`、`/login`、`/notes`、`/notes/new`、`/notes/:id/edit`、`/growth`
- 完成统一请求封装（Axios + `/api` 代理 + Cookie）
- 完成 Pinia 鉴权状态管理与路由守卫

### Step 3：后端基础能力搭建
- 完成统一响应体、分页结构、全局异常处理
- 完成 Security 基础配置
- 完成 MyBatis-Plus 分页插件配置
- 完成健康检查与基础运行链路

### Step 4：鉴权功能开发
- 完成注册、登录、登出、当前用户接口
- 完成 Session/Cookie 登录态方案
- 前端登录与注册页与后端接口联调通过

### Step 5：博客模块开发
- 完成笔记数据模型、Mapper、Service、Controller
- 完成笔记 CRUD + 分页 + 状态筛选 + 关键词筛选
- 前端完成笔记列表与编辑页，打通创建/编辑/删除流程

### Step 6：成长图谱模块开发（一期）
- 完成成长统计接口（时间线、标签聚合）
- 前端完成时间线图和雷达图展示
- 图表组件使用模块化引入，降低主包体积压力

### Step 7：构建与环境问题修复
- 修复 TypeScript 版本冲突（升级到 `~5.8.0`）
- 修复后端若干编译问题（构造器/泛型推断/getter 缺失）
- 补充 Maven 本地配置（settings 与本地仓库）
- 完成数据库配置切换（`blog_web/root/123456`）并重测

### Step 8：前端视觉重构
- 完成应用壳层、登录页、笔记列表页、笔记编辑页、成长页统一视觉
- 统一暖色阅读风格、非对称双栏布局、短句文案
- 图表背景与配色已与页面风格对齐

### Step 9：公开首页与登录门槛改造
- 新增公开首页 `/`，顶部导航可见
- 未登录点击受保护功能显示“请登录后使用”并可跳转登录
- 后端未登录文案统一为“请先登录”

### Step 10：随笔（动态）模块落地
- 前端新增随笔导航、随笔列表页、随笔编辑页
- 前端新增 `moment` API 与类型定义
- 后端新增 `moment` 模块（Entity/Mapper/Service/Controller/Request/VO）
- 新增迁移脚本：`V2__add_moment_table.sql`

### Step 11：首页视觉二次重构（参考 `web_style.jpg`）
- 首页改为海蓝沉浸式首屏 + 波浪过渡 + 左右内容流布局
- 顶部导航新增首页态样式（`is-home`），保留原有路由与鉴权行为
- 首页入口保留受保护功能门槛提示“请登录后使用”
- 首屏文案区改为居中布局，并移除首屏快捷按钮（登录后发随笔 / 先看成长图谱）
- 响应式断点补齐（1199 / 767）并通过前端构建验证

### Step 12：全站视觉统一与导航滚动隐藏收口
- 导航统一为跨页面同策略（下滑隐藏、上滑恢复、顶部常显）
- 博客/随笔/成长/登录页按首页视觉语言统一（海蓝系、轻卡片层级、短句文案）
- 内页容器与卡片边框、阴影、间距体系收口，保持非对称主布局
- 修复样式尾部结构问题并完成前端构建验证（`npm --prefix frontend run build`）

### Step 13：登录页二次重构（参考 `denglu.jpg`）
- 登录页复用首页导航态，保持顶部主入口视觉一致
- 登录区改为沉浸式全屏随机背景，每次进入页面随机切换背景图
- 登录与注册卡片改为图片与表单叠层组合，并按登录与注册态联动缩放

### Step 14：首页主用户信息与站点统计接入
- 后端新增主用户公开接口：`GET /api/auth/main-user`
- 后端新增站点统计接口：`GET /api/site/stats`
- 新增迁移脚本：`V4__add_site_counter.sql`（站点访问量计数）
- 首页“知行记录者”模块改为统计卡片（文章数 / 标签去重数 / 访问量）

### Step 15：成长图谱二阶段扩维（闭环分析）
- 后端成长接口扩展闭环数据：产出（博客/随笔创建）、反馈（博客评论/随笔评论/随笔点赞）、迭代（博客更新）
- 新增闭环摘要与闭环趋势线数据结构，统一由 `GET /api/growth/overview` 返回
- 前端成长页新增闭环指标展示，并支持与区间筛选（30 / 90 / 120 天）联动刷新

### Step 16：成长页信息架构收口
- 闭环趋势线由条形列表升级为三序列折线图（产出/反馈/迭代）
- 折线图日期标签优化为稀疏展示并强制显示首尾，提升可读性
- 右侧卡片收口为“状态结构 + 个人成就 + 内容总量 + 创作历程（按年）”
- 个人成就改为四行明细（博客点赞/博客评论/随笔点赞/随笔评论），并加图标分组

### Step 17：二期社交模块第一段（已完成）
- 已完成：随笔互动能力中的点赞 / 取消点赞、评论新增 / 删除（前后端联调通过）
- 已完成：随笔列表交互升级（搜索图标、筛选弹窗、条件分组折叠）
- 已完成：随笔可见范围第一版（公开 / 仅自己 / 圈层）能力落地，并完成三身份可见性验证（未登录 / 主用户 / 非主用户）
- 待完成：关注、私信能力

---

## 4. 当前实现状态

### 4.1 已可用能力
- 用户注册、登录、登出
- 笔记新增、编辑、删除、查询、筛选
- 随笔新增、编辑、删除、查询、筛选
- 随笔互动：点赞/取消点赞、评论新增/删除
- 成长主时间线、技能雷达、周内活跃分布、状态结构
- 闭环分析（产出/反馈/迭代）与闭环趋势线（三序列折线图）
- 公开首页 + 登录门槛提示交互
- 首页视觉重构（海蓝首屏、波浪过渡、非对称内容流、移动端断点）
- 全站视觉统一与导航滚动隐藏策略（下滑隐藏、上滑恢复、顶部常显）
- 首页“知行记录者”统计卡片（文章数 / 标签去重数 / 访问量）
- 首页主用户资料与站点统计接口（`/api/auth/main-user`、`/api/site/stats`）

### 4.2 当前需处理问题
1. ECharts 相关 chunk 仍偏大，后续继续拆分优化
2. 终端使用 `curl -d` 发送中文 JSON 可能出现编码问题（建议优先前端或 Postman 验证）
3. 全站需按 375 / 768 / 1440 三档做人工视觉验收，确认统一风格与导航滚动行为稳定
4. 成长页“个人成就”中的博客点赞暂未接入后端数据源，当前固定展示为 0
5. 二期社交模块仍缺少关注与私信能力

---

## 5. 现阶段回归测试清单

### 后端
1. 启动后端服务
2. 验证注册、登录、当前用户、登出接口
3. 验证笔记 CRUD 与分页筛选
4. 验证随笔 CRUD、点赞/取消点赞、评论新增/删除
5. 验证成长统计接口返回结构（含 `loopSummary` / `loopTimeline`）

### 前端
1. 首页导航与登录门槛提示
2. 登录与注册流程
3. 笔记列表筛选 + 删除
4. 随笔列表筛选 + 互动（点赞/评论）+ 删除
5. 新建/编辑笔记保存
6. 新建/编辑随笔保存
7. 成长页区间切换（30 / 90 / 120 天）
8. 成长页闭环趋势线（三序列折线图）与右侧信息卡联动
9. 响应式检查（1440 / 1280 / 1024 / 768 / 390）

---

## 6. 后续开发步骤安排（执行顺序）

## 阶段 A：稳定性收口（优先级 P0）
**目标**：保证当前一期能力可稳定交付。

1. 修正后端端口配置并完成启动自检
2. 补齐关键接口参数校验与异常提示一致性
3. 补充一期主链路回归测试脚本（接口 + 前端）
4. 输出一期发布检查清单

**验收标准**：
- 本地可一键启动前后端
- 主链路无阻断问题
- 构建与基础回归全部通过

## 阶段 B：社交基础模块（二期第一段，P1）
**目标**：实现最小互动闭环。

1. 圈层可见范围（CIRCLE）能力补齐（在已完成公开/仅自己基础上扩展）
2. 关注/取关/关注列表
3. 私信会话与发送消息基础能力

**验收标准**：
- 互动数据可读可写
- 页面可完成完整用户动作闭环

## 阶段 C：低干扰机制（二期第二段，P1）
**目标**：在“可交流”与“不打扰”间平衡。

1. 提醒分级（弱提醒默认）
2. 安静时段设置
3. 通知汇总与延迟投递

**验收标准**：
- 默认策略可降低打扰频次
- 用户可配置提醒偏好并生效

## 阶段 D：后台管理与治理（P2）
**目标**：支持运营与管理。

1. 用户管理（状态、权限）
2. 内容管理（审核、下线）
3. 基础权限管理

## 阶段 E：安全、性能、部署（P2）
**目标**：上线准备。

1. 安全加固：输入校验、权限边界、限频
2. 性能优化：索引、慢查询分析、接口压测
3. 部署：生产 MySQL 配置、容器化与发布流程

---

## 7. 建议的近期执行计划（1~2周）

### Week 1
- 完成阶段 A（稳定性收口）
- 补齐文档化测试步骤

### Week 2
- 推进阶段 B（社交模块）
- 优先推进关注最小闭环（关注/取关/关注列表），再进入私信基础能力

---

## 8. 维护约定

- 每完成一个阶段，更新本文件“已完成步骤”和“当前状态”
- 每新增需求，先补充到“后续开发步骤安排”再实施
- 与 `README.md`、`TODO.md`、`CLAUDE.md` 保持一致，避免信息分叉

